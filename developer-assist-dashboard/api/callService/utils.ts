developer-assist-dashboard/api/callService/utils.ts

/**
 * Utility function to log error messages.
 * @param {string} message - The error message.
 */
export function logError(message: string): void {
  console.error(message);
}
